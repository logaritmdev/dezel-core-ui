# Dezel C/C++ shared UI code.

This library contains the core logic for UI related code such as layout and
styles. Each platforms must provides their own wrappers for this to work
properly.

Licence: MIT